

import React from 'react';

// This component is deprecated and has been removed as per the refactoring plan
// to consolidate dashboard creation logic into a single shared component.
// The active component is now located at `components/AddDashboardModal.tsx`.
const AddDashboardModal: React.FC = () => null;

export default AddDashboardModal;
