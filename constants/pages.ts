// This file is DEPRECATED as of v2.31 and should NOT BE USED.
// All UI content is now served from the `/api/v1/ui/content` endpoint
// and consumed through the `useContent` hook from `ContentContext.tsx`.
// The content has been migrated to `mock-server/db.ts` to act as the data source for the mock API.